{
  "name": "chronicle",
  "version": "1.0.0",
  "description": "Velocity-Based Training web application for squat tracking",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
